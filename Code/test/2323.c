#include<stdio.h>
int main()
{
    char c;
    int i;
    while((c=getchar())!=EOF)
    {
         if(c=='\n') continue;
        else if(c==' ') continue;
        else if(c=='{')
        {
        }
        }
    }
